@model PMS.Models.ForgotPasswordViewModel
@{
    ViewBag.Title = "Forgot Password";
    Layout = "~/Views/Shared/_LayoutAccount.cshtml";
}

<style>
    .btn-sub {
        background-color: #76CED2;
        color: white;
        transition: all .3s ease-out;
    }

        .btn-sub:hover {
            background-color: #70c1c4;
            color: white;
        }
</style>

<div>
    <h4 class="font-size-18 mt-5 text-center">Reset Your Password</h4>
    <p class="text-muted text-center">Let's Identify your email</p>
</div>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <hr />

        <div class="form-group">
            @Html.EditorFor(model => model.Email, new { htmlAttributes = new { @class = "form-control col-12", @placeholder = "Email" } })
            @Html.ValidationMessageFor(model => model.Email, "", new { @class = "text-danger" })
        </div>

        <input type="submit" value="Reset Your Password" class="btn btn-sub col-12" />
    </div>
}

<p class="text-center mt-5 pt-4">Remembered your password? @Html.ActionLink("Sign In", "SignIn", "Account", null, new { @class = "font-weight-medium text-primary" })</p>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}


